package pet.articles.test.controller.constant

object ControllerTestConstants {

    object Fields {
        const val USERNAME = "username"
        const val EMAIL = "email"
    }

    object JsonPaths {
        const val PATH = "$"
        const val FIELD = "$.field"
        const val ID = "$.id"
        const val DATE_OF_CREATION = "$.dateOfCreation"
        const val ERROR = "$.validationError"
        const val LENGTH = "$.length()"
    }
}